# Drop your documents here

Supported formats: **`.md`**, **`.txt`**, **`.json`**, **`.pdf`**

NéoMêtis auto-indexes this folder into Qdrant on every startup (and every 60s while running).

Example: copy a spec, README, or PDF — then ask the agent about it in the chat.
